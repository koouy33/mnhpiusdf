apiVersion: v1
kind: Service
metadata:
  name: okiugyu
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "okiugyu"
    port: 8080
  selector:
    app: okiugyu
